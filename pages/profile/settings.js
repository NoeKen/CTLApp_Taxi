import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const Setting = () => {
  return (
    <SafeAreaView>
        <Text>Setting</Text>
    </SafeAreaView>
  );
};

export default Setting;Setting