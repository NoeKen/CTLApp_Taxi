import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const VehiclesList = () => {
  return (
    <SafeAreaView>
        <Text>VehiclesList</Text>
    </SafeAreaView>
  );
};

export default VehiclesList;